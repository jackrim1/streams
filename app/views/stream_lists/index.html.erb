<%= @stream_lists.each do |stream_list| %>
  <div class="index_row clearfix">
    <h2 class="stream_list_title">
      <%= link_to stream_list.title, stream_list %></h2>
    <p class="stream_list_sub_title">
      <%= stream_list.description %></p>
  </div>
<% end %>

<div class="links">
  <%= link_to "New Stream List", new_stream_list_path %>
</div>